#include "main.h"

/**
 * _isupper - a program that returns uppercase
 *
 * @c: the parameter for c
 * Return: return 1 if true and 0 if false
 */

int _isupper(int c)
{
	if (c >= 'A' && c  <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
