#include "main.h"

/**
 * _isdigit - a program that prints out number from 0-9
 * * @c: parameter that for isdigit function
 * Return: return 1 if true and 0 otherwise
 */

int _isdigit(int c)
{
	if (c >= 48  && c <= 57)
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
